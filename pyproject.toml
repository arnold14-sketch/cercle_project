[project]
name = "cercle"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pip>=26.1.2",
]

[dependency-groups]
dev = [
    "ruff>=0.15.4",
]
testing = [
    "pytest>=9.0.2",
]
